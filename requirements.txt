mcp[cli]  # Core MCP library for server and client
python-dotenv  # For managing environment variables via .env files (optional)
# NOTE: sqlite3 is included with Python's standard library and does not need to be installed via pip
